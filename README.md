# Backpropagation
Backpropagation

Use a neural network with two inputs, two hidden neurons, and two
output neurons. Additionally, the hidden and output neurons will include 
a bias.
In order to have some numbers to work with, here are the initial 
weights, the biases, and training inputs/outputs:

